*1- Modify models
*2- Meet point will be created in request creation form and that request will all it got will be sent for rides
*3- Images will be removed 



//make request -> saved in database 
//show all requests
//see available rides -> if there is accepted -> show it, else show suitable rides



// add license id in user table
// requests table should have meetpoint lat and meetpoint longitude(double) instead of string meetpoint + destination
// separate meetpoint to a seperated table that would have lat and lng points, meeting time, refer to request id
// rides table should have destination lat and destination longitude(double) instead of string destination + start



//request id , ride id , user_id  requestId and rideId cannot repeat